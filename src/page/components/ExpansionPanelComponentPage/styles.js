import { colors } from "config";

export default {
    host: {
    },
    content: {
        padding: 8,
        paddingBottom: 0,
        marginBottom: 32,
        borderWidth: 1,
        borderColor: "#f8f9fa"
    },
    badgeList: {
        paddingBottom: 8,
    }
};
